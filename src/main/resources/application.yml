spring:
  application:
    name: chulcheck
  datasource:
    url: jdbc:mysql://43.201.190.216:3306/chulcheck
    username: root
    password: 9oormthon.hufs4
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    root : info
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

springdoc:
  swagger-ui:
    path: swagger
    try-it-out-enabled: true
    filter: true
    operations-sorter: method
    display-request-duration: true
  override-with-generic-response: false